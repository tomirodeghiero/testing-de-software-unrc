{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd web && yarn install --frozen-lockfile",
  "buildCommand": "cd web && yarn run build",
  "outputDirectory": "web/build"
}
